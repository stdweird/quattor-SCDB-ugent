unique template common/bacula/packages;

prefix "/software/packages";

# ugbaculaclient and bacula-client as dependency
"{ugbaculaclient-gpfspolicy}" = nlist();
"{m4}" = nlist();
"{perl-JSON}" = nlist();
