unique template common/aquilon/packages;

prefix "/software/packages";

# Deps of aquilon itself
"{git-daemon}" = dict();
"{aquilon-protocols}" = dict();
"{ant-contrib}" = dict();
"{PyYAML}" = dict();
"{zip}" = dict();

# Deps of aquilon-protocols
"{protobuf-python}" = dict();
"{protobuf}" = dict();

"{java-1.6.0-openjdk}" = dict();

"{python-psycopg2}" = dict();
"{aquilon}" = dict();
"{knc}" = dict();
"{panc}" = dict();
"{aquilon-build-xml}" = dict();
"{aquilon-profile}" = dict();
